import 'package:flutter/material.dart';

class WriteRecipeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Tulis Resep')),
      body: Center(child: Text('Write Recipe Page')),
    );
  }
}
